# Rules for AI

This file provides guidance to Claude Code when working with code in this repository.

@AGENTS.md

## Claude Code Notes

- Keep Claude-specific guidance here only; shared repository rules live in `AGENTS.md`.
- `.claude/skills` is a junction to `.agents/skills`, so Claude Code and Codex use the same shared skill catalog.
- If Claude-only settings or workflow notes are needed later, keep them under `.claude/` and reference them from here.
